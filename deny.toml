# cargo-deny 配置（T-45 CI：`cargo deny check`）。
# 如新增依赖引入未在 allow 中的 license，CI 会失败；确认无碍后在此补充，或加 exceptions。

[advisories]
# 已知漏洞/advisory 库默认拉取 RustSec；`yanked` 先告警不阻断，避免上游偶发撤销打断 CI。
yanked = "warn"
ignore = [
    # 由 utoipa/utoipa-gen 间接引入；当前无安全升级路径，仅标记为 unmaintained。
    "RUSTSEC-2024-0370",
    # rustls-pemfile 已归档（unmaintained）；当前作为 tunnel-server 的直接依赖使用。
    "RUSTSEC-2025-0134",
]

[licenses]
version = 2
# 常见 Rust 依赖许可。若 CI 报未覆盖的 license，确认后加入此表或 exceptions。
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "MPL-2.0",
    "Unicode-DFS-2016",
    "Unicode-3.0",
    "Zlib",
    "OpenSSL",
    "CC0-1.0",
    "CDLA-Permissive-2.0",
]
# copyleft = "deny"      # 如需禁止 GPL 等传染性许可，取消注释

[bans]
multiple-versions = "warn"
wildcards = "deny"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
