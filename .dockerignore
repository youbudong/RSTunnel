# Docker 构建上下文排除项（context = 仓库根，避免把 target/ 等大目录上传给 daemon）。

# Rust 构建产物（巨大）
target/

# 前端：构建上下文仅需源码；node_modules/dist 在镜像内重新生成
web/node_modules/
web/dist/

# 模糊测试（非 workspace 成员）
fuzz/

# VCS / 文档 / 编辑与工具目录
.git/
.claude/
docs/
*.md
