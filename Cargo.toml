[workspace]
resolver = "2"
members = [
    "crates/tunnel-common",
    "crates/tunnel-protocol",
    "crates/tunnel-config",
    "crates/tunnel-auth",
    "crates/tunnel-db",
    "crates/tunnel-metrics",
    "crates/tunnel-core",
    "server",
    "agent",
    "cli",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
rust-version = "1.88"

# ---- 内部 crate（path 依赖）----
[workspace.dependencies]
tunnel-common = { version = "0.1.0", path = "crates/tunnel-common" }
tunnel-protocol = { version = "0.1.0", path = "crates/tunnel-protocol" }
tunnel-config = { version = "0.1.0", path = "crates/tunnel-config" }
tunnel-auth = { version = "0.1.0", path = "crates/tunnel-auth" }
tunnel-db = { version = "0.1.0", path = "crates/tunnel-db" }
tunnel-metrics = { version = "0.1.0", path = "crates/tunnel-metrics" }
tunnel-core = { version = "0.1.0", path = "crates/tunnel-core" }

# ---- 外部依赖（版本在 workspace 统一）----
# async runtime
tokio = { version = "1", features = ["full"] }
futures-util = "0.3"

# serialization / config
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
# 配置快照导入/导出（T-42，§100）用 YAML。serde_yaml 已归档，若 T-45 audit 告警再换维护中的 fork。
serde_yaml = "0.9"

# transport / tls / http（server 数据面后续启用）
quinn = { version = "0.11", default-features = false, features = ["rustls", "runtime-tokio"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12", "logging"] }
tokio-rustls = { version = "0.26", default-features = false, features = ["ring", "logging", "tls12"] }
rustls-pemfile = "2"
x509-parser = "0.17"
rcgen = "0.13"
axum = { version = "0.7", features = ["ws"] }
# 静态文件服务（server 内嵌 Web 管理后台，T-24/§153）。0.5 匹配 axum 0.7 的 tower 0.4；
# 0.6 依赖 tower 0.5，与 axum 0.7 的 `Service` trait 不兼容。
tower-http = { version = "0.5", features = ["fs"] }
hyper = { version = "1", features = ["full"] }
httparse = "1"
utoipa = { version = "4", features = ["axum_extras"] }
utoipa-swagger-ui = { version = "7", features = ["axum"] }

# database（v1 默认 SQLite；PostgreSQL 在 HA 里程碑启用）
sqlx = { version = "0.8", features = ["runtime-tokio", "sqlite", "migrate", "macros"] }

# crypto
argon2 = "0.5"
sha2 = "0.10"
rand = "0.8"

# observability
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["json", "env-filter"] }
# 禁用默认 `protobuf` feature：它只为 OpenMetrics/Protobuf 编码（我们只用 TextEncoder），
# 且会引入 protobuf 2.x（RUSTSEC-2024-0437 未修复）。default-features=false 即可去掉该依赖。
prometheus = { version = "0.13", default-features = false }

# ids / time / bytes
uuid = { version = "1", features = ["v4", "serde"] }
time = { version = "0.3", features = ["serde", "formatting", "macros", "parsing"] }
bytes = "1"

# error handling / misc
thiserror = "1"
anyhow = "1"
async-trait = "0.1"
dashmap = "6"
arc-swap = "1"
clap = { version = "4", features = ["derive"] }

# 全局 lint 约定
[workspace.lints.rust]
unsafe_code = "forbid"

[workspace.lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
panic = "deny"
